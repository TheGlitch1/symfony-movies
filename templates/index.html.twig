{# <h1>
Welcome to this new twig page symfony</h1>
<div>
    {{ title }} <br>
    {{ dump(title) }}

    {% if title %}
        <p>{{title}} is a movie</p>
    {% else %}
        <p>No title has been set</p>
    {% endif %}

</div> #}

{% extends 'base.html.twig' %}
{# Override the block title we already have on the index  #}
{% block title %}
    Movies Page
{% endblock %}

{% block body %}
    <h1 class="bg-blue-500 text-2xl text-center font-bold">
        {{ title }}
    </h1>
    <img src="{{ asset('build/images/image1.57fc3c43.jpg')}}" alt="">
    {# {% for movie in movies %}
        <li>{{movie}}</li>
    {% endfor %} #}
{% endblock %}